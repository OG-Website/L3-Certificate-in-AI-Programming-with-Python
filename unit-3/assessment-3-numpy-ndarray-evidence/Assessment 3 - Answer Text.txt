Q1. Give a general description of the use of NumPy

NumPy is a third-party Python library used for numerical computing and array-based data work. Its full name is Numerical Python, and it is one of the main libraries used when Python needs to handle numbers in a structured and efficient way. Standard Python can store numbers in lists, but lists are general-purpose objects. NumPy is designed specifically for numerical data, so it gives Python a much stronger way to store, calculate, reshape, save and reuse arrays of values.

The main object used in NumPy is the ndarray, which means n-dimensional array. An ndarray can store data in one dimension, like a single list of numbers, two dimensions, like a table of rows and columns, or more dimensions for more complex data. This makes NumPy useful for many types of programming work, including data analysis, scientific computing, artificial intelligence, machine learning, statistics, image processing and engineering calculations. In Unit 3, the ndarray is important because the assessment asks me to create and save one using NumPy in Jupyter Notebook.

NumPy is used when data needs to be handled as a set of values rather than as separate individual variables. For example, a learner could store test scores, prices, measurements, pixel values, sensor readings or statistical data inside an ndarray. Once the values are in an ndarray, NumPy can perform operations across the whole array. This is more practical than writing repeated code for every value. If two arrays have matching shapes, NumPy can add, subtract, multiply or divide their values element by element. This makes numerical work shorter, clearer and easier to check.

Another use of NumPy is inspecting the structure of data. An ndarray has properties such as shape, ndim, size and dtype. Shape shows the rows and columns or wider structure of the array. ndim shows how many dimensions the array has. Size shows the total number of values stored in it. dtype shows the data type, such as integer or floating-point number. These properties are useful because numerical programs need to know what kind of data they are processing. In my Assessment 3 notebook, I use these properties to prove that the ndarray has been created correctly.

NumPy is also used for preparing data before it is used by other libraries. Pandas, for example, can work with NumPy arrays and often uses NumPy internally. This means NumPy is not just an isolated library. It supports wider Python data workflows. In Unit 3, NumPy links naturally to the later Pandas work because NumPy handles numerical arrays while Pandas handles labelled table data. A NumPy ndarray can be used as a starting point before converting or analysing the data in another format.

NumPy is also useful because arrays can be saved and loaded again. A program can create an ndarray, process it, and then save it to a file. NumPy provides different saving methods. np.savetxt() can save the array as readable text or CSV-style data, while np.save() stores the array in NumPy's own .npy format. The .npy format is useful because it preserves the array structure and data type. Loading the file back later proves that the data was not just temporary screen output.

In this assessment, I use NumPy in Jupyter Notebook to create a two-dimensional ndarray called og_scores. The notebook imports NumPy, creates the ndarray with np.array(), checks its properties, saves it as .txt, .csv and .npy files, and then loads those files back into Python. The screenshots and GitHub links provide evidence that NumPy was used practically, not only described in writing.

Word count: 627 words

Q2. Describe two or more benefits of using NumPy (3.2).

Benefit 1 - efficient numerical processing

One benefit of using NumPy is that it makes numerical work in Python more efficient and easier to manage. Standard Python lists can store numbers, but they are general-purpose structures and are not designed mainly for numerical processing. NumPy arrays are built for this kind of work. The main NumPy structure is the ndarray, which stores values in a regular array format. This means I can work with a whole set of numbers as one structured object instead of treating every value separately.

In my Assessment 3 work, this is shown in the Jupyter Notebook where I create an ndarray called og_scores. The array stores two rows of score data. Instead of creating separate variables for each score, the values are held together in one NumPy array. This makes the data easier to print, check, save and load back later. It also makes the code more professional because the array has a clear name, a clear structure and a clear purpose. This links to the OG Web.site evidence approach because the screenshot does not just show a random piece of code. It shows an organised notebook cell, comments explaining the code, and output proving the array was created.

Benefit 2 - clear data structure and checking

A second benefit of using NumPy is that it gives clear information about the structure of the data. NumPy arrays have useful properties such as shape, ndim, size and dtype. These help explain what the array contains before it is used further. Shape shows the layout of the array, such as two rows and three columns. ndim shows the number of dimensions. Size shows the total number of values, and dtype shows the type of data stored in the array. These properties are useful because they let me check that the ndarray has been created correctly.

This is important in my Unit 3 evidence because the assessment is not only asking me to say what NumPy is. It asks me to use NumPy practically. In the notebook, I print the ndarray properties after creating og_scores. That gives evidence that I understand the structure of the array and can prove it through output. In the OG Web.site evidence pack, this screenshot can be annotated to explain that the array is two-dimensional, contains six values and stores integer data. This makes the evidence stronger because it shows both the code and the result.

Benefit 3 - saving, reusing and evidencing the data

A third benefit of NumPy is that it allows numerical data to be saved and reused. If an array only appears as notebook output, it disappears as soon as the session is closed unless the notebook is saved. NumPy gives commands that save the actual array data into files. In my Assessment 3 notebook, I use np.savetxt() to save the ndarray as readable .txt and .csv files. I also use np.save() to save the ndarray in NumPy's own .npy format. This is useful because the saved files become evidence files that can be included with the submission.

This links directly to the way I have organised my OG Web.site evidence. The notebook creates the ndarray, the saved files prove that the array was written to disk, and the GitHub folder shows those files uploaded with the notebook. I then load the files back using np.loadtxt() and np.load() to prove they work. That creates a complete evidence chain: create the ndarray, check it, save it, upload it, and prove it can be loaded again.

Another benefit is that NumPy fits well with wider Python library work. Pandas and other data libraries often work with NumPy-style data. This matters because Unit 3 is about Python libraries, not just one isolated command. NumPy supports the practical foundation for later Pandas work, where structured data can be turned into Series or DataFrames. Overall, NumPy is beneficial because it makes numerical work faster, clearer, more organised, easier to evidence, and easier to connect to other Python data tools.

Word count for Q2 total: 652 words
