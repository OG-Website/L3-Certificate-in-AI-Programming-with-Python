Q3. Use the third-party library NumPy, to create and save an ndarray (Unit J/650/0758 4.1, 4.3). Ensure you explain how to create and save ndarrays within the comments, or using markdown cells in Jupyter Notebook (3.3).

I used the third-party library NumPy to create and save an ndarray in a Jupyter Notebook. I imported NumPy with import numpy as np, then created a two-dimensional ndarray called og_scores using np.array([[82, 76, 91], [68, 88, 95]], dtype=int). The notebook comments explain that each inner list becomes one row and that dtype=int stores the values as integers.

I checked the ndarray by printing the array and its properties: shape, ndim, size and dtype. This proves the ndarray was created correctly as a two-dimensional array with two rows, three columns, six values and an integer data type.

I saved the ndarray in three formats. I used np.savetxt to save readable .txt and .csv evidence files, and I used np.save to save a NumPy .npy file. I then loaded the saved files back into Python with np.loadtxt and np.load to prove the saved ndarray files worked correctly.

Notebook evidence:
https://github.com/OG-Website/L3-Certificate-in-AI-Programming-with-Python/blob/main/unit-3/og-website-numpy-evidence-pack/02%20Jupyter%20Notebook/OG%20Web.site%20Unit%203%20NumPy%20ndarray%20Evidence.ipynb
